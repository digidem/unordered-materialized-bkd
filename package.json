{
  "name": "unordered-materialized-bkd",
  "version": "1.1.1",
  "description": "materialized view spatial tree based on unordered log messages",
  "dependencies": {
    "bkd-tree": "^1.0.1",
    "once": "^1.4.0"
  },
  "devDependencies": {
    "concat-map": "0.0.1",
    "memdb": "^1.3.1",
    "random-access-memory": "^3.0.0",
    "tape": "^4.9.0"
  },
  "scripts": {
    "test": "tape test/*.js"
  },
  "license": "BSD"
}
